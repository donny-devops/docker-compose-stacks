interface:
  display_name: "Docker Compose Stacks"
  short_description: "Repo-specific patterns and workflows for docker-compose-stacks"
  default_prompt: "Use the docker-compose-stacks repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true