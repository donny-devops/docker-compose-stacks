version: 2
updates:
  # Keep GitHub Actions used by the workflows up to date (a maintenance bot).
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    commit-message:
      prefix: chore(deps)
    labels:
      - dependencies
      - github-actions
    open-pull-requests-limit: 5
